module.exports = async (client, oldMessage, newMessage) => {
    const chn = client.channels.find("id", "551808050743476244");
    
    if (oldMessage.channel.id === "551492101305794561"){
        chn.send(
            {embed: {
                color: 3447003,
                title: oldMessage.author.username + "#" + oldMessage.author.discriminator + " - Campaign message update",
                fields: [{
                    name: "__Previous__",
                    value: oldMessage.content
                },
                {
                    name: "__Current__",
                    value: newMessage.content
                },
		{
		}
                ],
                timestamp: new Date(),
            }
            }
        );
    }
}
